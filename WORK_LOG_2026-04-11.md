# Work Log — 2026-04-11

## 1. Sales Chatbot — Website AutoChatbot

**Files changed:**
- `app/api/chatbot/route.ts`
- `components/AutoChatbot.tsx`

**Kya kiya:**
- Website ke existing AutoChatbot mein sales functionality add ki
- Sales intent detect karta hai: buy, purchase, demo, trial, get started, interested, sign up, etc.
- 3-step sales flow:
  1. Bot naam poochta hai
  2. Email poochta hai
  3. Plan interest poochta hai (Basic / Medium / Premium)
- Email capture hone ke baad lead `sales_leads` Supabase table mein save hoti hai
- `salesData` state `AutoChatbot.tsx` mein track hota hai across conversation turns
- Quick actions mein "🚀 Get Started" button add kiya

**Supabase tables used:**
- `sales_leads` (already created by user)
- `sales_conversations` (already created by user)

---

## 2. Chatbot Type Field — Agent Creation Form

**Files changed:**
- `app/(user)/Agent/page.tsx` (frontend)
- `app/schema.py` (backend)
- `app/main.py` (backend)
- `app/agent.py` (backend)

**Kya kiya:**
- Agent creation form mein **"Chatbot Type"** dropdown add kiya (Tone ke neeche)
- 3 options:
  - 🤖 General Bot — custom AI assistant
  - 🎧 Support Bot — issues resolve, ticket escalate
  - 💰 Sales Bot — lead capture, plan recommend, conversions
- Har option ke neeche description text dikhta hai
- `type` field Supabase `agents` table mein save hota hai
- `type` backend ko bhi bheja jaata hai `/create-agent` API call mein

**Supabase SQL (run karna zaroor hai):**
```sql
ALTER TABLE public.agents
ADD COLUMN IF NOT EXISTS type TEXT NOT NULL DEFAULT 'general'
CHECK (type IN ('general', 'support', 'sales'));
```

---

## 3. Type-Based System Prompts — Backend

**File changed:** `app/agent.py`

**Kya kiya:**
- `TYPE_PROMPTS` dict add kiya with 3 different system prompt personalities:
  - **sales** → convert visitors, capture leads, recommend plans, CTA
  - **support** → resolve issues, collect ticket info, escalate
  - **general** → helpful assistant, stay on topic
- `run_agent()` function ab `agent_data['type']` read karke matching system prompt inject karta hai
- Agar `type` nahi mila toh default `general` use hota hai

---

## 4. Plan-Based Form Visibility — Agent Creation

**File changed:** `app/(user)/Agent/page.tsx`

**Kya kiya:**
- Pehle saare sections dikhte the (locked/disabled) — confusing tha
- Ab plan ke hisaab se sirf relevant fields dikhte hain:

| Plan | Visible Fields |
|------|---------------|
| **Basic** | Name, Description, Tone, Type + Upgrade hint |
| **Medium** | Basic + Welcome Message, Response Length, Knowledge Base |
| **Premium** | Medium + System Prompt, Custom CSS, Webhook URL |

- Locked/disabled fields hata diye — sirf upgrade hint dikhta hai Basic users ko
- Code 105 lines se 80 lines ho gaya (cleaner)

---

## Git Commits Today

| Repo | Commit | Message |
|------|--------|---------|
| autobot-studio | `d02957b` | Feat: add sales lead capture to website chatbot |
| autobot-studio | `29c16ed` | Feat: add chatbot type selector to agent creation form |
| autobot-studio | `dfadc0b` | Fix: pass type field to backend on agent creation |
| autobot-studio | `ba20f5b` | Feat: show agent form fields based on plan |
| autobot-backend | `a22d32e` | Feat: type-based system prompts for sales/support/general agents |

---

## Pending / Notes

- Supabase SQL for `type` column must be run manually if not done yet
- `sales_leads` table must exist in Supabase (user created it earlier)
- Backend on Render will auto-redeploy from GitHub push
- Frontend on Vercel will auto-redeploy from GitHub push
